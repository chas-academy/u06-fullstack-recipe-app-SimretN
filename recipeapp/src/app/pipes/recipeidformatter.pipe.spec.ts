import { RecipeidformatterPipe } from './recipeidformatter.pipe';

describe('RecipeidformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeidformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
